/**---------------------------------------------------------------------------------------------------------------------
 * A plain old JavaScript object that can be serialized into JSON
 *--------------------------------------------------------------------------------------------------------------------*/

export type Json = Json.Primitive | Json.Object | Json.Array;

/**---------------------------------------------------------------------------------------------------------------------
 * A plain old immutable JavaScript object that can be serialized into JSON
 *--------------------------------------------------------------------------------------------------------------------*/

export type ReadonlyJson = Json.Primitive | Json.ReadonlyObject | Json.ReadonlyArray;

/**---------------------------------------------------------------------------------------------------------------------
 * @mergeModuleWith Json
 *--------------------------------------------------------------------------------------------------------------------*/

export namespace Json {
    /**-----------------------------------------------------------------------------------------------------------------
     * A primitive JSON data type
     *----------------------------------------------------------------------------------------------------------------*/

    export type Primitive = undefined | null | boolean | number | string;

    /**-----------------------------------------------------------------------------------------------------------------
     * A JSON object
     *----------------------------------------------------------------------------------------------------------------*/

    export interface /* NOSONAR */ Object {
        [index: string]: Json;
    }

    /**-----------------------------------------------------------------------------------------------------------------
     * A JSON array
     *----------------------------------------------------------------------------------------------------------------*/

    export type Array = (Exclude<Primitive, undefined> | Object | Json.Array)[]; // NOSONAR

    /**-----------------------------------------------------------------------------------------------------------------
     * A JSON readonly object
     *----------------------------------------------------------------------------------------------------------------*/

    export interface ReadonlyObject {
        readonly [index: string]: ReadonlyJson;
    }

    /**-----------------------------------------------------------------------------------------------------------------
     * A JSON readonly array
     *----------------------------------------------------------------------------------------------------------------*/

    export type ReadonlyArray = readonly (Exclude<Primitive, undefined> | Object | Json.ReadonlyArray)[];
}
